name: Release

on:
  push:
    branches: [main]
    paths:
      - packages/mock-dash/package.json
  workflow_dispatch:

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      issues: write
      pull-requests: write

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.MY_GITHUB_TOKEN }}

      - uses: actions/setup-node@v4
        with:
          node-version: lts/*

      - name: Install pnpm
        run: npm install -g pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Get core package version
        id: package-version
        run: echo "version=$(node -p "require('./packages/mock-dash/package.json').version")" >> $GITHUB_OUTPUT

      - name: Check if tag exists
        id: tag-exists
        run: |
          if git rev-parse "v${{ steps.package-version.outputs.version }}" >/dev/null 2>&1; then
            echo "exists=true" >> $GITHUB_OUTPUT
          else
            echo "exists=false" >> $GITHUB_OUTPUT
          fi

      - name: Run tests (core package)
        if: steps.tag-exists.outputs.exists == 'false'
        run: pnpm -F mock-dash test

      - name: Run linting (core package)
        if: steps.tag-exists.outputs.exists == 'false'
        run: pnpm -F mock-dash lint

      - name: Build core package
        if: steps.tag-exists.outputs.exists == 'false'
        run: pnpm -F mock-dash build

      - name: Create Release
        if: steps.tag-exists.outputs.exists == 'false'
        run: |
          gh release create "v${{ steps.package-version.outputs.version }}" \
            --title "Release v${{ steps.package-version.outputs.version }}" \
            --generate-notes
        env:
          GITHUB_TOKEN: ${{ secrets.MY_GITHUB_TOKEN }}
